# Identifier autofill implementation baseline

The September 9 multi-identifier plan is being integrated into the current capture and claims candidate, `d81093a`, on `feat/multi-identifier-autofill-20260909`. The GitHub default branch fetched during inspection was still `d8dfe24`; rebuilding from that older branch would omit the current capture, palette and claims work. The retained source includes those changes and has no tracked modifications at start.

The selected web site reports version 17 at `https://thepackproof.com`, updated September 9. The live API health check succeeds and its capabilities response reports `18f7511fb121604d5db12805cfe2ab919eb482f3`, version `2026-09-09-preproduction-claims`. This reconciles the later claims deployment with its older handoff document. Source migration head is 059; the live database migration head is not inferred from the capabilities response. The physical installed Android revisions are unavailable in this environment; their certification is a pilot gate.

The ordinary Android path is `recordPackingEvidence` → existing CameraX `UnifiedCameraView` → shipping journal → `completeSavedCapture` → one platform attestation → recovery coordinator. The native decoder is bundled ML Kit 17.2.0. Expo remains 52 / React Native 0.76.9. The browser path is `PackingStationScreen` → the existing MediaRecorder stream → IndexedDB recovery → `resumeStationRecording`. Capture engine v1 remains a separate, existing optional manifest extension; it is not made a prerequisite for identifier enrichment.

Shared pure identifier contracts will live beside the existing shared capture core in `backend/src/identifiers`. The ordinary capture session remains authoritative. New session policy, observation journal, resolution events and checkpoint records are additive. Old shipping filters remain shipping-specific. New product observations must pass scoped server resolution before any shipping association is attempted.

Focused gates cover identifier parsing and queue recovery; scoped resolution, replay, review and sealing; affected web capture/recovery and renderer tests; mobile capture/recovery/typecheck; production build and native compilation. Physical camera, installed-browser and real-data pilot results must be recorded separately from synthetic fixture results. No live provider or performance qualification is inferred from passing unit tests.
